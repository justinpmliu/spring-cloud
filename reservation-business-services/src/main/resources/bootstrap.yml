spring:
  application:
    name: reservation-business-services
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      username: conf
      password: ${config_password}
eureka:
  client:
    service-url:
      defaultZone: http://eureka:${eureka_password}@eureka1:8761/eureka/,http://eureka:${eureka_password}@eureka2:8762/eureka/