spring:
  application:
    name: room-services
  cloud:
    config:
      uri: https://conf:${config_password}@myhost:9000

eureka:
  client:
    service-url:
      defaultZone: https://eureka:${eureka_password}@myhost:8761/eureka/